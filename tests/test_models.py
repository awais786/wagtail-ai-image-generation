"""
Placeholder for Phase 2 model tests.

AIGeneratedImage (one-to-one with Wagtail Image) is introduced in v0.2.
Tests for prompt, provider, model, style, size, requesting user, timestamp,
and API response status fields will live here.
"""
# No tests yet — models.py has no database models in v0.1.
